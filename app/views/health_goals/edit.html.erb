<div class="mx-auto md:w-2/3 w-full">
  <%= render partial: "shared/active_record_errors", locals: { resource: @health_goal } %>
  <h1>Editing health goal</h1>
  <%= render "form", health_goal: @health_goal %>
  <div class="inline-block ml-2">
    <%= link_to "Show this health goal", @health_goal, class: "rounded-lg py-3 px-5 bg-gray-100 inline-block font-medium" %>
    <%= link_to "Back to health goals", health_goals_path, class: "rounded-lg py-3 px-5 bg-gray-100 inline-block font-medium" %>
  </div>
</div>
