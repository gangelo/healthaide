<% content_for :title, "Forgot password" %>
<div class="mx-auto md:w-2/3 w-full">
  <h1>Forgot your password?</h1>
  <p class="text-gray-600 mb-6">Enter your email address below and we'll send you instructions to reset your password.</p>
  <%= form_for(resource, as: resource_name, url: password_path(resource_name), html: { method: :post }, class: "contents") do |f| %>
    <%= render "shared/active_record_errors", resource: resource %>
    <div class="my-5">
      <%= f.label :email, "Email address", class: "block font-medium text-gray-700 mb-1" %>
      <%= f.email_field :email,
                      autofocus: true,
                      autocomplete: "email",
                      required: true,
                      placeholder: "Enter your email address" %>
    </div>
    <div class="button-group-container">
      <%= f.submit "Send reset instructions", class: "submit-button" %>
      <%= link_to "Back to sign in", new_session_path(resource_name), class: "back-button whitespace-nowrap" %>
    </div>
  <% end %>
  <div class="mt-6 text-center">
    <%= render "devise/shared/links" %>
  </div>
</div>
